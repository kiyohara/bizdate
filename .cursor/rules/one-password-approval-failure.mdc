---
description: 1Password 連携を伴う git / GitHub / MCP 操作が承認待ちで失敗したときに読む中断と報告のルール
alwaysApply: false
---

# 1Password 承認待ち失敗

- 詳細は `doc/guidelines/one-password-approval-failure.md`
- 1Password 連携操作の前にゲートや preflight（`op whoami`、`ssh-add -l` など）を置かない。通常どおり実行する。
- 承認待ちの失敗（タイムアウト / 拒否 / lock / プロンプト未到達など）を検知したら中断し、失敗した操作・エラー要旨・未反映の変更を報告して選択肢を提示する。
- `op run` を使う MCP server の起動失敗（`CONNECT_TIMEOUT` / `Connection closed` / 接続中のまま進まない）も対象。原因は host に出ないので、`docker info` など 1Password に触れない確認で原因が見つからなければ中断する。
- 別の 1Password 連携経路へ自動 fallback しない。署名の無効化、鍵・remote・credential helper の無断変更、1Password app の更新・再インストール・再起動もしない。
