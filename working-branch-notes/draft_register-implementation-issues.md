# 作業ブランチメモ

- ブランチ: `register-implementation-issues`
- PR: （採番前）
- 最終更新: 2026-09-06

## 目的

v1 実装タスクとして登録した GitHub Issue #7〜#15 を、`progress.md` の進行中タスク索引へ登録する。

`register-progress-issue` skill に従い、起点 Issue を作らず独立 PR として出す。

## 現在の状況

索引表の追加と現況・次にやることの更新を完了した。PR 作成待ち。

## 決定事項

### 索引に載せる単位

#7〜#15 は v1 実装という 1 つの横断プランを構成するため、新しい表を 1 つ作って 9 件すべてを載せる。`development-loop.md` の「横断的に見渡したい単位」に合致する。

Issue #5（skill の commit / PR 扱いの追記）は登録しない。closed かつ単発 Issue であり、skill の step 2 の「closed / 既に完了済みの Issue は登録しない」に該当する。

### 依存の書き方

依存欄には**必須条件のみ**を書き、単なる推奨順は含めない。表の並び順が推奨順を表す。

- #8〜#10 は #7 のみに依存する。技術的には #9 と #10 を並行できるが、本リポジトリは直列消化を基本とする（`development-loop.md`）。
- #11 は #9 と #10 の両方に依存する。`--day-off` の `MM-DD` を採用 TZ の「今日」の年で解決するため #9 が要り、祝日集合を使うため #10 が要る。
- #13 は #10（ファイル形式と保存場所の共有）と #12（サブコマンドの骨組み）に依存する。

### `progress.md` の他セクションの扱い

現況と「次にやること」は、整備完了前の記述のまま stale になっている（「次は開発環境の整備に進む」「整備完了後、実装タスクを登録し」）。実装索引を追加すると内部矛盾するため、**最小限の更新に留めて整合を取る**。

**開発環境整備の表そのものは圧縮しない。** 全項目 done の表をフェーズ要約へ畳むのは `maintain-progress` skill の観点 3 の責務であり、本 skill のスコープ外である。別途 `maintain-progress` を実行する。

## 次にやること

- `progress.md` に v1 実装の索引表を追加する。
- 現況と「次にやること」を最小限更新する。
- PR を作成し、note を採番する。

## 検証

skill の step 8 に沿って確認した。

- **各セクションの役割**: 現況 → 進行中タスク（開発環境整備 / v1 実装）→ 次にやること → 完了 → 後続で追加する guideline、の構成が保たれている。現況の stale（「次は開発環境の整備に進む」）を解消し、実装索引と矛盾しない状態にした。
- **索引から Issue へ辿れる**: 9 行すべてに Issue リンクがあり、#7〜#15 が漏れなく並んでいることを確認した。
- **依存欄が必須条件のみ**: V1-01 が `-`、V1-02〜04 が `#7`、V1-05 が `#9, #10`、V1-06 が `#11`、V1-07 が `#10, #12`、V1-08 が `#12, #13`、V1-09 が `#14`。推奨順は表の並びで表し、依存欄に混ぜていない。
- **詳細経緯が複製されていない**: 各行は 120〜165 文字で、Issue 本文の背景・スコープ外・検証を含まない。
- **秘密情報**: 検出なし。
- 開発環境整備の表の #4 を `done（PR merge 後）` から `done` へ更新した。PR #4 は merge 済みで、事実の追随である。

## リスク・ブロッカー

- なし。

## セッションログ

- 2026-09-06: main から分岐し、本 note を作成した。Issue #7〜#15 が OPEN・関連 PR なしであることを MCP で確認済み。
- 2026-09-06: `progress.md` に v1 実装の索引表（V1-01〜V1-09）を追加。現況と「次にやること」を更新し、開発環境整備表の #4 を done に追随させた。
