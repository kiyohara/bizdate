# 作業ブランチメモ

- ブランチ: register-ci-latency-issues
- PR: #66
- 最終更新: 2026-09-23

## 目的

PR ごとの CI の待ち時間を縮めるために起票した #64 と #65 を、`progress.md` の配布準備の索引へ登録する。`register-progress-issue` による索引登録だけの独立 PR であり、起点 Issue を持たない。

## 現在の状況

- #64（配布対象から Intel Mac を外す）と #65（cargo-about を prebuilt で導入し、PR での CI の二重実行をやめる）を起票した。
- #39 の「依存」を #38 から #65 に改めた。Issue 本文の更新であり、この PR の差分には含まれない。
- `progress.md` の配布準備の表に 2 行を加え、#39 の依存、現況、次にやることを更新した。

## 決定事項

- 経緯: PR #62 の最終 push で Release workflow が 8 分 14 秒かかり、律速は Intel Mac の build job だった。検討の結果、ユーザーが Intel Mac を配布対象から外すと判断した（2026-09-23）。案内は「Apple Silicon のみ対応」の記載だけとし、手動ビルドはサポートせず、再追加は検討しない。判断の詳細は #64 の本文を正とする。
- 順序: #64 → #65 → #39 → #40 の直列とする。
  - #64 は配布契約（対象 target）を変えるため、Formula を作る #39 と案内を書く #40 より先に置く。
  - #65 は、prebuilt が全 build runner にそろう #64 の後でないと成立しない。
  - #39 は #65 の後に置く。#39 の PR でも push ごとに build job が走るため、先に待ち時間を縮めておく。
- ID: 既存の DIST-04 / DIST-05 を振り直さず、DIST-03 と DIST-04 の間に DIST-03a / DIST-03b として挿入する。索引の変更を最小限にとどめ、既存 ID の参照を崩さないためである。
- #63（Cargo の Dependabot）は従来どおり索引に載せない単発の Issue とし、今回の順序に組み込まない。

## 次にやること

- [x] PR を作成し、note を採番する
- 索引登録 PR のレビューと merge の後、`run-issue-task` で #64 に着手する。

## 検証

- #64 / #65 を読み戻し、title、state（open）、節の構成、相互参照を確かめた。
- #39 の本文を読み戻し、依存の節だけが意図どおり変わっていることを byte 単位で確かめた。
- `progress.md` を冒頭から読み、各節の役割、表の列、Issue / PR へのリンク、依存欄の整合を確かめた。
- `git diff --check`、note の情報統制（禁則の実値なし）。
- コードと CI は変更しないため、Compose の検証は行わない。

## リスク・ブロッカー

- #39 の本文には、#64 で 3 target の前提へ同期するまで 4 target の記述が残る。依存の節にその旨を書いた。

## セッションログ

- 2026-09-23: macOS の CI の待ち時間を検討し、ユーザーの判断で Intel Mac を外す方針に決めた。#64 / #65 を起票し、#39 の依存を更新した。索引登録の branch と note を作り、`progress.md` を更新した。
